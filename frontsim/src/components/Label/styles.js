import styled from 'styled-components';

import { metrics } from '~/styles';

export const LabelCpt = styled.label`
    font-style: normal;
    font-weight: 600;
    font-size: ${metrics.fontSize.extraSmall}px;
    line-height: 19px;
`;