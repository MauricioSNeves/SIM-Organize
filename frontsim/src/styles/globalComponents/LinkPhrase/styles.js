import styled from 'styled-components';

import { metrics } from '~/styles';

export const LinkPhrase = styled.text`
    color: rgb(61, 57, 63, 0.4);
    font-size: ${metrics.fontSize.extraSmall}px;
`;
