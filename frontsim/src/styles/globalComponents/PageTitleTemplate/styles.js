import styled from 'styled-components';

import { colors, metrics } from '~/styles';

export const PageTitleTemplate = styled.h1`
    margin-top: 50px;
    margin-bottom:20px;
    color: ${colors.gray};
    font-size: ${metrics.fontSize.extraBig}px;
`;
